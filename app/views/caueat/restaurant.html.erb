<script>
var myCenter=new google.maps.LatLng(<%= @restaurant.mapx %>, <%= @restaurant.mapy %>);

function initialize()
{
var mapProp = {
  center:myCenter,
  zoom:17,
  mapTypeId:google.maps.MapTypeId.ROADMAP
  };

var map=new google.maps.Map(document.getElementById("googleMap"),mapProp);

var marker=new google.maps.Marker({
  position:myCenter,
  });

marker.setMap(map);
}

google.maps.event.addDomListener(window, 'load', initialize);
</script>

<div class="container" style="align:center;">
    <h3 class="center-align">중앙대 밥집?</h3>
      <div class="row">
        <div class="col s12">
          <div class="card grey lighten-2">
            <div class="card-content black-text">
              <h2 class="center-align"><%= @restaurant.title %></h2>
              <h2 class="center-align">(<%= @restaurant.mapx %>,<%= @restaurant.mapy %>)</h2>
              <div id="googleMap" style="width:500px;height:380px;"></div>
            </div>
            <div class="card-action">
                <a class="waves-effect waves-light btn-large" href="/">뭐먹지?</a>
                <a class="waves-effect waves-light btn-large" href="/caueat/pub">술집은?</a>
            </div>
          </div>
        </div>
      </div>
</div>

